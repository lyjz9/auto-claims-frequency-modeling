# Data

This project uses synthetic auto insurance policyholder data generated by the pipeline.

- `raw/synthetic_auto_claims_10k_realistic.csv`: generated synthetic policyholder records
- `processed/auto_claims_analysis_clean.csv`: cleaned and feature-engineered modeling table

Run this from the project root to regenerate both files:

```bash
python auto_claims_pipeline.py --all
```
