# Outputs

Generated analysis outputs are saved here:

- `figures/`: portfolio-ready PNG charts
- `tables/`: CSV summary tables
- `model_summaries.txt`: statistical test and model summaries

Run this from the project root to refresh the outputs:

```bash
python auto_claims_pipeline.py --all
```
